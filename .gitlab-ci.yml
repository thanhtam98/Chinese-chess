image: mcr.microsoft.com/vscode/devcontainers/cpp:0-bullseye

build:
  stage: build
  script:
    - cmake -S . -B build
    - cmake --build build -j $(nproc)
  artifacts:
    paths:
      - build/chinese-chess

test:
  stage: testgi
  # before_script:

  script:
    - cd build && ctest
